<h1>My tasks</h1>

<ol>
  <% @tasks.each do |task| %>
    <li><input type='checkbox'><%= link_to task.title, task_path(task.id) %></li>
  <% end %>
</ol>

<p><%= link_to 'Add a new task', task_path(task.new) %></p>
